import { ErButton, ErButtonGroup, ErIcon, ErCollapse, ErCollapseItem, ErAlert } from "@toy-design-ui/components"
import type { Plugin } from "vue"

export default [
    ErButton, ErIcon, ErButtonGroup, ErCollapse, ErCollapseItem, ErAlert
] as Plugin[];